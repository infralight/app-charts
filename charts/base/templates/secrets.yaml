---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: external-secret-{{ .Values.appTemplate.appName }}-secrets
  namespace: {{ .Values.appTemplate.namespace }}
spec:
  data:
    {{- if .Values.secrets }}
    {{- include "base.tplvalues.render" ( dict "value" .Values.secrets "context" $ ) | nindent 2 }}
    {{- end }}
    #- remoteRef:
    #    conversionStrategy: Default
    #    key: mongodb
    #    property: MONGO_URI
    #  secretKey: MONGO_URI
  refreshInterval: 1h0m0s
  secretStoreRef:
    kind: SecretStore
    name: {{ .Values.appTemplate.namespace }}.vault-backend
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: {{ .Values.appTemplate.appName }}-secrets
