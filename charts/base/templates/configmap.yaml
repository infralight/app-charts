apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.appName }}
data: 
    {{- if .Values.configMap }}
    {{- include "base.tplvalues.render" ( dict "value" .Values.configMap "context" $ ) | nindent 2 }}
    {{- end }}